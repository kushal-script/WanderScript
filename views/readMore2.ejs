<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= post.title %> - WanderScript
    </title>
    <link rel="stylesheet" href="/css/profile.css" />
    <link rel="stylesheet" href="/css/readMore.css" />
</head>

<body>
    <nav class="navbar">
        <a href="/"><img src="/assets/icons8-home-50.png" alt="home"></a>
        <a href="/WanderScript/profile" class="username">@<%= post.username %></a>
    </nav>

    <div class="profile-wrapper">
        <div class="post-card">
            <h2 style="color:#00f0ff; font-size: 1.6rem;">
                <%= post.title %>
            </h2>
            <p style="margin-top: 1rem; font-size: 1rem; line-height: 1.6;">
                <%= post.info %>
            </p>

            <div class="post-footer" style="margin-top: 2rem;">
                <form action="/WanderScript/posts/upvote/<%= post._id %>" method="POST">
                    <button class="action-btn upvote-btn" type="submit">
                        <% if (post.upvotedBy.includes(currentUserId)) { %>
                            🔽 Remove Upvote
                            <% } else { %>
                                ⬆️ Upvote
                                <% } %>
                                    (<%= post.upvotes %>)
                    </button>
                </form>
            </div>
        </div>
    </div>
</body>

</html>