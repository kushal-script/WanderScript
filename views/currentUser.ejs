<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WanderScript - Profile</title>
    <link rel="stylesheet" href="/css/profile.css" />
</head>

<body>
    <nav class="navbar">
        <a href="/"><img src="/assets/icons8-home-50.png" alt="home"></a>
        <span class="username">@You%></span>
        <div class="icons">
            <a href="<%= user.linkedin %>"><img src="/assets/linkedin_3536569.png" alt="linkedin"></a>
            <a href="<%= user.instagram %>"><img src="/assets/instagram_1400829.png" alt="instagram"></a>
            <a href="<%= user.youtube %>"><img src="/assets/youtube_1384028.png" alt="youtube"></a>
        </div>
    </nav>

    <div class="profile-wrapper">
        <div class="profile-header">
            <div class="bio">
                <h3>Bio</h3>
                <p>
                    <%= user.bio %>
                </p>
            </div>

            <div class="stats">
                <div><strong>Posts</strong> <br>
                    <%= user.posts.length %>
                </div>
                <div><strong>UpVotes</strong> <br>
                    <%= user.totalUpvotes %>
                </div>
                <div><strong>Followers</strong> <br>
                    <%= user.followers.length %>
                </div>
            </div>

            <div class="new-post-card">
                <a href="/WanderScript/posts/new">
                    <div class="post-icon">➕</div>
                    <div class="post-text">Post</div>
                </a>
            </div>
        </div>

        <div class="action-buttons">
            <a href="/profile/edit" class="action-btn edit-profile">✏️ Edit Profile</a>
            <a href="/dashboard" class="action-btn dashboard">📊 Dashboard</a>
        </div>

        <% user.posts.forEach(post=> { %>
            <div class="post-card">
                <h4>
                    <%= post.title %>
                </h4>
                <p>
                    <%= post.info.substring(0, 150) %>...
                </p>
                <a href="/posts/<%= post._id %>" class="read-more">Read more...</a>
                <div class="post-footer">
                    <span>Blog Up Votes ⬆️ <%= post.upvotes %></span>
                    <div class="post-actions">
                        <a href="/posts/edit/<%= post._id %>" class="btn-edit">✏️ Edit Post</a>
                        <form action="/posts/delete/<%= post._id %>" method="POST" style="display:inline;">
                            <button type="submit" class="btn-delete">🗑️ Delete Post</button>
                        </form>
                    </div>
                </div>
            </div>
            <% }) %>
    </div>
</body>

</html>