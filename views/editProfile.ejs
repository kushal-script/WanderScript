<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Profile - WanderScript</title>
  <link rel="stylesheet" href="/css/profile.css" />
  <link rel="stylesheet" href="/css/editProfile.css"/>
</head>
<body>
  <nav class="navbar">
    <a href="/WanderScript/profile" class="username">@<%= user.username %></a>
  </nav>

  <div class="profile-wrapper">
    <div class="post-card">
      <h2 style="color:#00f0ff; font-size: 1.5rem;">✏️ Edit Profile</h2>

      <% if (typeof message !== 'undefined' && message) { %>
        <p style="color: rgb(0, 243, 77); margin: 1rem 0;"><%= message %></p>
      <% } %>

      <form action="/WanderScript/profile/edit?_method=PUT" method="POST" style="display: flex; flex-direction: column; gap: 1rem; margin-top: 1rem;">
        <input
          type="text"
          name="username"
          value="<%= user.username %>"
          placeholder="Username"
          required
          style="padding: 0.8rem; border-radius: 10px; border: 1px solid #888; background-color: #121212; color: white;"
        />

        <textarea
          name="bio"
          placeholder="Tell us about yourself..."
          rows="4"
          style="padding: 0.8rem; border-radius: 10px; border: 1px solid #888; background-color: #121212; color: white;"
        ><%= user.bio %></textarea>

        <input
          type="url"
          name="linkedin"
          value="<%= user.linkedin %>"
          placeholder="LinkedIn URL"
          style="padding: 0.8rem; border-radius: 10px; border: 1px solid #888; background-color: #121212; color: white;"
        />

        <input
          type="url"
          name="instagram"
          value="<%= user.instagram %>"
          placeholder="Instagram URL"
          style="padding: 0.8rem; border-radius: 10px; border: 1px solid #888; background-color: #121212; color: white;"
        />

        <input
          type="url"
          name="youtube"
          value="<%= user.youtube %>"
          placeholder="YouTube URL"
          style="padding: 0.8rem; border-radius: 10px; border: 1px solid #888; background-color: #121212; color: white;"
        />

        <div class="action-buttons">
          <button type="submit" class="action-btn dashboard">✅ Save Changes</button>
          <a href="/WanderScript/profile" class="action-btn edit-profile">🔙 Back to Profile</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>