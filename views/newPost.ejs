<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>New Post - WanderScript</title>
  <link rel="stylesheet" href="/css/profile.css" />
  <link rel="stylesheet" href="/css/newPost.css"/>
</head>
<body>
  <nav class="navbar">
    <a href="/WanderScript/profile" class="username">@<%=user.username%></a>
  </nav>

  <div class="profile-wrapper">
    <div class="post-card">
      <h2 style="color:#00f0ff; font-size: 1.5rem;"> Create a New Post</h2>

      <% if (typeof message !== 'undefined') { %>
        <p style="color: crimson; margin: 1rem 0;"><%= message %></p>
      <% } %>

      <form action="/WanderScript/posts/new" method="POST" style="display: flex; flex-direction: column; gap: 1rem; margin-top: 1rem;">
        <input
          type="text"
          name="title"
          placeholder="Enter your post title"
          required
          style="padding: 0.8rem; border-radius: 10px; border: 1px solid #888; background-color: #121212; color: white;"
        />

        <textarea
          name="description"
          placeholder="Write your post content here..."
          rows="8"
          required
          style="padding: 0.8rem; border-radius: 10px; border: 1px solid #888; background-color: #121212; color: white;"
        ></textarea>

        <div class="action-buttons">
          <button type="submit" class="action-btn dashboard"><img src="/assets/icons8-new-post-24.png" alt="post"> Post</button>
          <a href="/WanderScript/profile" class="action-btn edit-profile"><img src="/assets/icons8-back-64.png" alt="back"> Back to Profile</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>