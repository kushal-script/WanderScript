<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sign In</title>
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <div class="auth-card active">
        <h2>WanderScript</h2>
        <form action="/WanderScript/signin" method="POST">
            <div class="form-box">
                <label>username</label>
                <input type="text" name="username" required>

                <label>e-mail id</label>
                <input type="email" name="email" required>

                <label>password</label>
                <input type="password" name="password" required>

                <button type="submit">sign in</button>
            </div>
        </form>
        <div style="text-align: center; margin-top: 1rem;">
            <p style="font-size: 0.9rem; margin-bottom: 0.5rem;">Forgot your password?</p>
            <a href="/WanderScript/forgot-password">
                <button style="padding: 0.5rem 1rem; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer;">
                    Reset Password
                </button>
            </a>
        </div>
        <div style="text-align: center; margin-top: 1rem;">
            <p style="font-size: 0.9rem; margin-bottom: 0.5rem;">New user?</p>
            <a href="/WanderScript/signup">
                <button style="padding: 0.5rem 1rem; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer;">
                    Sign Up
                </button>
            </a>
        </div>
        <div style="text-align: center; margin-top: 1rem;">
            <p style="font-size: 0.9rem; margin-bottom: 0.5rem;">Or sign in another way:</p>
            <a href="/WanderScript/verify-otp">
                <button class="secondary-btn">Sign in through OTP</button>
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if (message === 'User not found! Redirecting to sign up.') { %>
        <script>
          Swal.fire({
            title: 'WanderScript',
            text: "User not found! Redirecting to sign up.",
            icon: "info",
            confirmButtonColor: '#007bff'
          });
        </script>
        <% } %>
        <% if (message === 'Let the blogging begin!') { %>
            <script>
              Swal.fire({
                title: 'WanderScript',
                text: "Let the blogging begin!",
                icon: "success",
                confirmButtonColor: '#007bff'
              });
            </script>
            <% } %>
            <% if (message === 'Incorrect credentials.') { %>
                <script>
                  Swal.fire({
                    title: 'WanderScript',
                    text: "Incorrect credentials.",
                    icon: "error",
                    confirmButtonColor: '#007bff'
                  });
                </script>
                <% } %>
</body>
</html>